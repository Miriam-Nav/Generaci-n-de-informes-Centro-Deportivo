<Window x:Class="View.ActividadesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
                Title="Gestión de Actividades"
        Height="680" Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F7">


    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Estilos.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- BARRA SUPERIOR -->
        <Border Style="{StaticResource BarraSup}">
            <DockPanel>
                <TextBlock Text="Gestión de Actividades" Style="{StaticResource TituloBarraStyle}" DockPanel.Dock="Left"/>

            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- FORMULARIO -->
            <Border Grid.Column="0" Style="{StaticResource TarjetaStyle}">

                <StackPanel>

                    <TextBlock Text="Datos de la Actividad" Style="{StaticResource TituloTarjetaStyle}"/>

                    <!-- Nombre -->
                    <TextBlock Text="Nombre:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding InputNombreActividad}" Style="{StaticResource TextBoxStyle}"/>
                    <TextBlock Text="{Binding ErrorNombre}" Style="{StaticResource ErrorMensajeStyle}"/>

                    <!-- Aforo -->
                    <TextBlock Text="Aforo Máximo:" Style="{StaticResource LabelStyle}" Margin="0,15,0,0"/>
                    <TextBox Text="{Binding InputAforoMaximo}" Style="{StaticResource TextBoxStyle}"/>
                    <TextBlock Text="{Binding ErrorAforo}" Style="{StaticResource ErrorMensajeStyle}"/>

                    <!-- Botones -->
                    <StackPanel Margin="0,25,0,0">
                        <Button Content="Crear"
                            Command="{Binding CrearCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>

                        <Button Content="Modificar"
                            Command="{Binding ModificarCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>

                        <Button Content="Eliminar"
                            Command="{Binding EliminarCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>

                        <Button Content="Limpiar"
                            Command="{Binding LimpiarCommand}"
                            Style="{StaticResource LimpiarButtonStyle}"/>
                    </StackPanel>

                    <TextBlock Text="{Binding ErrorActividad}" Style="{StaticResource ErrorMensajeStyle}" Margin="20,20,20,0"/>

                </StackPanel>
            </Border>

            <!-- LISTA -->
            <Border Grid.Column="2" Style="{StaticResource ListaStyle}">
                <DataGrid ItemsSource="{Binding Actividades}"
                      SelectedItem="{Binding ActividadSeleccionada}"
                      Style="{StaticResource DataGridStyle}"
                      Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" MinWidth="250"/>
                        <DataGridTextColumn Header="Aforo" Binding="{Binding AforoMaximo}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </Grid>
    </Grid>
</Window>
